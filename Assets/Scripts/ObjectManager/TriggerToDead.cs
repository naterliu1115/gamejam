using UnityEngine;

public class TriggerToDead : MonoBehaviour
{

    //Health health;
    
    void Start()
    {
        //health = GetComponent<Health>();
    }

    /*
    void OnTriggerEnter2D(Collider2D other)
    {
        if (other.Compare("Player"))
        {
            health.currentHP = 0;
        }
    }*/
}
